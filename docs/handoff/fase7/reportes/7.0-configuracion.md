# 7.0 — Configuración externa: Twilio, WhatsApp y Resend

- **Fecha:** 2026-09-16
- **Commit:** ver `git log` del commit "docs(fase7): …"
- **Estado:** hecha
- **Bloqueada por:** nada

## Qué se configuró y definió

Se consolidó el estado de las cuentas externas y los parámetros no sensibles para la Fase 7 (Twilio WhatsApp, Sandbox y Resend) con base en la información del usuario y `01-configuracion-twilio-whatsapp.md`.

## Resumen de Configuración

| Sección | Parámetro | Valor / Estado | Notas |
|---|---|---|---|
| **A. Twilio** | Tipo de cuenta | *Trial* | Solo envía a números verificados o sandbox. |
| **A. Twilio** | Account SID | en `.env` local | Identificador, no secreto; no hace falta en el repo. |
| **A. Twilio** | Credenciales | `TWILIO_AUTH_TOKEN` en local `.env` | Indispensable para validar la firma `X-Twilio-Signature`. Salientes pueden usar API Key. |
| **B.1 WhatsApp** | Modo | **Sandbox** | Remitente: `whatsapp:+14155238886`. |
| **B.1 WhatsApp** | Teléfono prueba | `+59******418`, valor real solo en `.env` | Registrado enviando código `join <palabras>` al sandbox. |
| **B.2 WhatsApp** | Producción | Aplazado | Requerirá WABA verificado en Meta + Sender en Twilio. |
| **C. Plantillas** | Confirmación cita | `HX79aa0b881e10346eb8e34ccf507d5b23` | `notifications_appointment_confirmation_template` (Quick Reply). Nota: requiere verificación de idioma (`es` vs `en`). |
| **C. Plantillas** | Sandbox genérica | `HXb5b62575e6e4ff6129ad7c8efe1f983e` | Variables `{{1}}` fecha, `{{2}}` hora. |
| **D. Webhook** | Ruta entrante | `POST /webhooks/twilio` | Validará `X-Twilio-Signature`. Status callback url se deja vacía (D12/D17). |
| **E. Resend** | Dominio | **Verificado** | Envío de emails salientes y `.ics` migra a Go (D10/D16). |
| **F. Tenant prueba** | Slug / Cliente | `dev-test` / `v.pseudo.11@gmail.com` | Script `docs/handoff/sql/dev_test_tenant.sql`. |

## Variables requeridas en `.env` local

```bash
TWILIO_ACCOUNT_SID=AC...
TWILIO_AUTH_TOKEN=<AuthToken_secreto>
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886
TWILIO_CONTENT_SID=HX79aa0b881e10346eb8e34ccf507d5b23
TWILIO_WEBHOOK_URL=https://<tu-host-o-tunel>/webhooks/twilio
RESEND_API_KEY=re_<secreto>
FROM_EMAIL=Elvis Studio <notificaciones@tudominio-verificado.com>
```

## Siguiente paso

1. Dar de alta al especialista en Supabase Auth y ejecutar `dev_test_tenant.sql` en Supabase Studio.
2. Proceder con las tareas 7.2 (Endpoint webhook) y 7.5 (Parser de intención con botones y texto de respaldo).
