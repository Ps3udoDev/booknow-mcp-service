{
  "displayName": "booknow-mcp: Postgres no responde",
  "combiner": "OR",
  "conditions": [
    {
      "displayName": "algún error de Postgres en 5 min",
      "conditionThreshold": {
        "filter": "resource.type = \"cloud_run_revision\" AND resource.labels.service_name = \"${SERVICE}\" AND metric.type = \"logging.googleapis.com/user/booknow_mcp_db_errors\"",
        "aggregations": [
          {
            "alignmentPeriod": "300s",
            "perSeriesAligner": "ALIGN_SUM",
            "crossSeriesReducer": "REDUCE_SUM"
          }
        ],
        "comparison": "COMPARISON_GT",
        "thresholdValue": 0,
        "duration": "0s",
        "trigger": {
          "count": 1
        }
      }
    }
  ],
  "notificationChannels": [
    "${CHANNEL}"
  ],
  "alertStrategy": {
    "autoClose": "1800s"
  },
  "documentation": {
    "mimeType": "text/markdown",
    "content": "Falla `/readyz` o la resolución de tenant. Comprueba el estado de Supabase, el Session Pooler y el secreto `DATABASE_URL`.\n\nRunbook: docs/runbook-cloud-run.md (Observabilidad y Revertir)."
  }
}
