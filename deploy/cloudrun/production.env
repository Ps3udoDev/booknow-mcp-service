# Non-secret values for the production service. Secrets live in Secret Manager; only their names go here.
# GCP project agendia-mcp (docs/runbook-cloud-run.md, "Preparación única").
PROJECT_ID=agendia-mcp
PROJECT_NUMBER=248015398241
REGION=us-west1
SERVICE=booknow-mcp
APP_ENV=production
LOG_LEVEL=info
RUNTIME_SA=agendia-mcp-runner@agendia-mcp.iam.gserviceaccount.com
SUPABASE_URL=https://rrnysepngbycvuciodoj.supabase.co
# Custom domain (Cloud Run domain mapping, CNAME in Cloudflare as DNS only). It is also the OAuth protected resource:
# MCP clients must connect through this origin, since the run.app URL would not match the advertised resource.
MCP_PUBLIC_URL=https://mcp.agendia.store
# Browser origins allowed to call /mcp (comma-separated, no "*"). Empty = none: native and server-side
# clients send no Origin and are unaffected. Add an origin only after seeing a real client send it.
MCP_ALLOWED_ORIGINS=
MAX_INSTANCES=3
DB_MAX_CONNS=4
DATABASE_URL_SECRET=booknow-mcp-database-url
DATABASE_URL_SECRET_VERSION=1
